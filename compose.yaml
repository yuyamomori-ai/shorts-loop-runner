services:
  shorts-loop:
    build: .
    ports:
      - "127.0.0.1:8787:8787"
    env_file: .env
    environment:
      HOST: 0.0.0.0
      DATA_DIR: /app/data
      TRUST_LOOPBACK_PROXY: "true"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
